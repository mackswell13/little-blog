<div class="mt-10 max-w-7xl mx-auto px-4">
  <h1 class="text-4xl font-semibold text-center mb-8 text-headings">Blogs</h1>
  
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
    <% @blog_posts.each do |blog_post| %>
      <%= link_to blog_post, class: "bg-cards shadow-lg rounded-lg overflow-hidden transform hover:scale-105 transition-all duration-300" do %>
        <div class="p-4">
          <h2 class="text-2xl font-semibold text-primary mb-4"><%= blog_post.title %></h2>
          <!-- Blog Post Body (limit preview text) -->
          <p class="text-secondary text-sm line-clamp-3"><%= blog_post.body %></p>
        </div>
      <% end %>
    <% end %>
  </div>

  <!-- New Blog Button (visible only for authenticated users) -->
  <% if authenticated? %>
    <div class="mt-8 text-center">
      <%= button_to "New Blog",
        new_blog_post_path,
        method: :get,
        class: "bg-button text-background py-2 px-6 rounded-full text-lg hover:bg-blue-700 transition-all duration-200" %>
    </div>
  <% end %>
</div>

